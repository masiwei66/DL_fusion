# `data_new` 数据集结构与质量审计说明

## 1. 审计对象与结论

本次审计对象为 `E:/working/DL_data/data_new` 目录下的 `result_*.json` 文件，审计日期为 2026 年 8 月 6 日。本文档依据当前数据文件、`E:/working/DL_data/tmp/audit_data_new.py` 及其扩展分析结果编写。由于数据集仍在生成，以下数量统计只反映本次审计时的状态，后续新增样本后需要重新统计。

当前共有 11 个 JSON 文件，总大小约 16.68 MiB。所有文件均成功解析，统一使用 `4.0-temperature-state-json` schema，且均属于有限元域 `finite_element`。现有审计未发现 JSON 解析、数组形状、非有限值、时间轴、数组哈希、质量指标复算或标签一致性错误。因此，当前数据格式已经能够进入深度学习数据加载、训练和推理流程。

需要说明的是，格式检查通过并不代表数据规模已经满足正式实验要求。当前只有 11 个结构状态，健康状态和纯支座状态均只有 1 个样本，暂时只适合代码链路测试，不适合形成稳定的论文指标。

## 2. 样本定义与文件组织

数据集采用“一个 JSON 对应一个结构状态”的组织方式，文件名形式为：

```text
result_000000.json
result_000001.json
...
result_000010.json
```

每个样本的公共语义单元是：

```text
数据域 - 结构状态 - 温度阶段 - 测点/结构区域
```

`sample_index` 保存样本的主要索引。典型内容如下：

```json
{
  "sample_id": "fe_state_000000",
  "data_domain": "finite_element",
  "group_id": "state_000000",
  "structural_state_id": "state_000000",
  "condition_group": "damage_9_20_24__support_221_513_687",
  "sample_unit": "structural_state"
}
```

其中，`structural_state_id` 和 `group_id` 是后续划分训练集、验证集和测试集的关键字段。同一结构状态内部的温度步不得拆分到不同集合中。

## 3. 顶层数据结构

每个 JSON 包含 20 个顶层字段，其用途可概括为下表。

| 字段 | 类型 | 主要内容 | 训练中的用途 |
|---|---|---|---|
| `schema_version` | string | 数据协议版本 | 版本检查 |
| `iter` | integer | 样本序号 | 文件一致性检查 |
| `sample_index` | object | 样本、状态、分组和数据域索引 | 分组与追溯 |
| `dataset_roles` | object | 条件输入、响应输入和监督目标声明 | 输入/标签契约 |
| `environment` | object | 温度环境及温度步 | 模型输入 |
| `responses` | object | 静态位移、动态加速度、时间轴和激励力 | 模型输入 |
| `structural_state` | object | 场景类型及损伤/沉降状态摘要 | 分层统计与审计 |
| `dynamic_excitation` | object | 动力激励信息 | 审计或扩展输入 |
| `scaling_factors` | object | 有限元参数缩放记录 | 标签生成与审计 |
| `material_ids` | array | 全部材料 ID | 标签索引 |
| `material_scaling_factors` | array | 材料刚度缩放真值 | 监督目标，禁止作为输入 |
| `material_nodes` | object | 材料与节点的对应关系 | 区域映射和审计 |
| `support_settlement` | object | 支座沉降节点和幅值 | 监督目标 |
| `safety_labels` | object | 材料、支座、区域和全桥标签 | 监督目标 |
| `region_definitions` | object | 材料/支座到区域的映射 | 区域级任务 |
| `node_coords` | object | 节点三维坐标 | 位置编码 |
| `node_maps` | object | 节点 ID 到数组列索引的映射 | 张量读取 |
| `quality_metrics` | object | 响应和激励质量指标 | 质量审计；后续可用于门控 |
| `generation_config` | object | 样本生成参数 | 复现和审计 |
| `array_integrity` | object | shape、dtype 和 SHA-256 | 完整性检查 |

`dataset_roles` 已明确声明当前主模型的数据口径：

- 条件输入：`environment.temperature.temperature_steps_C`；
- 响应输入：`responses.static.disp` 和 `responses.dynamic.ace`；
- 监督目标：材料、支座、区域和全桥的分类/分级标签。

旧版应变数据不属于当前 V2 主输入。`material_scaling_factors`、`support_settlement` 和生成参数中包含结构状态真值，只能用于生成标签、辅助回归或审计，不能直接送入分类模型，否则会造成目标泄漏。

## 4. 静态响应数据

静态响应位于 `responses.static`，核心数组为：

```text
responses.static.disp: [6, 6, 3], float32, mm
```

三个维度依次表示：

1. 6 个温度步；
2. 6 个静态测点；
3. `UX`、`UY`、`UZ` 三个方向的位移。

静态节点为 `[43, 65, 87, 153, 175, 197]`，节点坐标随样本一同保存。每个样本从 10–15、15–20、20–25、25–30、30–35 和 35–40 °C 六个温度区间中分别抽取一个温度值。本批 11 个样本的实际温度范围为 10.2–39.5 °C，参考温度为 25 °C。`disp[i]` 与 `temperature_steps_C[i]` 一一对应。

本批静态位移最大绝对值范围为 0.463–7.644 mm。温度—位移线性拟合的最低决定系数仍高于 0.99999999997，说明当前均匀温度和线弹性设置下，静态响应基本呈仿射变化。这与生成条件一致，但也意味着材料损伤仅引起很小的温度斜率变化。已有扩展审计中，纯材料状态相对健康状态的温度斜率相对变化约为 `4.90e-5`–`2.78e-4`。因此，静态分支可能更容易识别支座沉降，而对单纯构件削弱的区分能力需要通过实验验证，不能预设其一定有效。

## 5. 动态响应数据

动态响应位于 `responses.dynamic`，主要数组如下：

| 数组 | shape | dtype | 单位 | 含义 |
|---|---:|---|---|---|
| `ace` | `[4000, 6, 3]` | float32 | mm/s² | 六测点三向加速度时程 |
| `time_s` | `[4000]` | float64 | s | 时间轴 |
| `force_N` | `[4000, 1]` | float32 | N | 动力激励力时程 |

动态节点为 `[23, 67, 133, 155, 177, 645]`，三个方向为 `AX`、`AY` 和 `AZ`。动态计算在 25 °C 参考平衡状态下进行，记录时长为 20 s，采样频率为 200 Hz，时间间隔为 0.005 s。激励施加于节点 120 的 `FZ` 方向，目标 RMS 为 10 N，频带为 2–40 Hz。

本批数据的整体加速度 RMS 范围为 124.147–147.757 mm/s²，峰值范围为 1345.938–1809.823 mm/s²，首尾时间窗 RMS 比值为 1.007–1.218，均处于生成配置给定的质量限制内。各测点/方向 RMS 差异较大，单个样本内部最大通道 RMS 比约为 73.47，因此训练时应使用仅由训练集估计的通道归一化，并在后续实验中检查测点失活和通道幅值不平衡的影响。

当前 11 个样本复用了同一条激励力时程，激励力 RMS 均为 10.0215 N。其优点是便于比较不同结构状态，缺点是不能证明模型对未见激励具有泛化能力。正式数据集应增加不同激励种子，或将“未见激励”作为独立测试条件。当前响应主频主要位于 23.8–39.85 Hz，并较多集中在激励频带上端，后续应检查模型是否学习了结构状态差异，而不是单纯记忆固定激励频谱。

## 6. 结构状态与监督标签

`structural_state.scenario` 将样本分为四类：

| 场景 | 当前数量 | 含义 |
|---|---:|---|
| `healthy` | 1 | 材料和支座均为正常状态 |
| `material_only` | 5 | 仅存在候选材料削弱 |
| `support_only` | 1 | 仅存在支座沉降 |
| `combined` | 4 | 材料削弱与支座沉降同时存在 |

### 6.1 材料标签

模型关注 6 个候选材料 ID：`[8, 9, 19, 20, 24, 41]`。`material_labels` 是长度为 6 的多标签二值数组，`material_risk_levels` 是对应的四级风险数组。当前正样本数量依次为：

| 材料 ID | 8 | 9 | 19 | 20 | 24 | 41 |
|---|---:|---:|---:|---:|---:|---:|
| 正样本数 | 5 | 8 | 4 | 4 | 4 | 6 |

材料风险等级由缩放因子确定：大于 0.90 为安全，`(0.80, 0.90]` 为预警，`(0.70, 0.80]` 为风险，小于等于 0.70 为危险。这里的缩放因子是有限元生成真值，不是实际工程中可直接观测的输入量。

### 6.2 支座标签

4 个支座节点为 `[221, 513, 395, 687]`。`support_labels` 是长度为 4 的二值数组，`support_risk_levels` 是四级风险数组。当前四个节点的正样本数分别为 3、4、4 和 4。支座沉降绝对值达到 2、5 和 7 mm 时，依次进入预警、风险和危险等级。

### 6.3 区域与全桥标签

数据集定义了 7 个结构区域：4 个支座区域，以及由材料 `[9,20]`、`[24,41]`、`[8,19]` 分别组成的右、中、左结构区域。`region_risk_levels` 的 shape 为 `[7]`，每个值属于 0–3 四个等级。当前共 77 个区域标签，其中安全 41 个、预警 4 个、风险 14 个、危险 18 个。

全桥标签 `global_level` 取所有区域风险等级的最大值。当前全桥状态分布为：安全 1 个、预警 3 个、风险 1 个、危险 6 个。该分布明显偏向危险状态，正式训练前需要继续补充健康、预警和风险样本。

## 7. 质量指标与完整性信息

`quality_metrics` 当前包含：

- 加速度绝对峰值、均值和 RMS；
- 首尾时间窗 RMS；
- 平稳性 RMS 比值；
- 激励力 RMS 及其相对误差。

这些指标目前主要用于生成后的质量验收，后续也可作为质量感知融合模型的门控输入。但在将其作为模型输入前，应确认这些指标在有限元、缩尺实验和实桥域中有统一含义与计算方法。

`array_integrity` 为静态位移、动态加速度、时间轴和激励力分别保存 shape、dtype 和 SHA-256。本次审计重新计算了全部哈希，11 个样本均与记录一致。该字段适合用于数据传输、归档和训练前的完整性检查。

## 8. 本次审计已通过的检查

本次对 11 个样本完成了以下检查，未发现错误：

- JSON 可解析，schema 和文件序号一致；
- `sample_id`、`structural_state_id` 和 `group_id` 格式一致；
- 六个温度步严格递增，温差和温度分箱正确；
- 静态位移、动态加速度、时间轴和激励力 shape 正确；
- 数组中不存在 NaN 或 Inf；
- 时间间隔、记录时长和采样频率一致；
- 数组 dtype、shape 和 SHA-256 与 `array_integrity` 一致；
- 加速度 RMS、峰值、平稳性和激励力 RMS 可由原始数组复算；
- 材料、支座、区域和全桥标签与设定阈值一致；
- 场景类型与实际启用的材料削弱和支座沉降一致。

## 9. 当前局限与训练前要求

当前数据集已经具备清晰、可追溯的 V2 格式，但正式实验前仍需解决以下问题：

1. **样本数量不足**：11 个结构状态无法支撑稳定的训练、验证和测试指标。
2. **类别不平衡**：健康和纯支座场景各只有 1 个，全桥安全/风险状态也各只有 1 个。
3. **激励多样性不足**：所有样本使用同一条激励力时程，不能评价未见激励泛化。
4. **数据域单一**：当前全部为有限元域，尚不能评价 FE 到缩尺实验或实桥的跨域能力。
5. **生成配置单一**：当前只有一套生成配置，边界条件、噪声和传感器差异尚未覆盖。
6. **静态材料信号较弱**：纯材料状态对温度斜率的影响很小，需要接受静态分支可能不敏感的实验结果。
7. **动态通道幅值不均衡**：不同通道 RMS 差异较大，必须使用训练集统计量归一化，并开展测点失活与噪声鲁棒性测试。

后续数据生成完成后，应重新运行审计脚本，冻结数据版本和样本清单，再按 `structural_state_id` 或 `group_id` 进行严格划分。归一化参数只能由训练集计算，阈值和概率校准参数只能由验证集确定，测试集不得参与调参。

## 10. 总结

`data_new` 已形成统一的温度状态 JSON 协议。每个样本同时包含多温度静态位移、参考温度下的动态加速度、空间位置、结构状态、分层安全标签、质量指标、生成配置和数组完整性信息，能够支持静态单模态、振动单模态和双分支融合模型的开发。当前格式和标签一致性检查均已通过，主要限制来自样本量、类别覆盖、固定激励和数据域单一。因此，现阶段最合适的用途是验证数据加载和训练流程；在形成论文实验结果前，需要继续扩充状态样本、激励种子和目标域数据。
