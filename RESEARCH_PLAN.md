# 桥梁多模态安全状态评估研究路线

本文档把论文框架转换成可执行的代码、数据和实验步骤。当前 `DL_model` 已切换到 V2 温度状态数据协议，默认读取 `E:/working/DL_data/data_new`。

## 0. 先冻结的研究口径

- 研究对象：桥梁安全状态辅助评估，不是通用图像识别或精确损伤反演。
- 公共样本单元：`data_domain - structural_state_id - stage/window - component/region`。
- V2 输入：`responses.static.disp`（多温度位移）、`environment.temperature`、`responses.dynamic.ace`（参考温度下加速度时程）、测点坐标、质量指标和缺测掩码。
- V2 监督：材料候选多标签、支座状态、区域风险等级、全桥风险等级；幅值回归作为辅助任务。
- `material_scaling_factors`、有限元生成参数和其他真值字段只能用于标签、分组或审计，不能作为模型输入。
- 主切分按 `structural_state_id`/`group_id`/实验批次进行；同一状态的温度步、滑动窗口和重复采集不得跨集合。
- 关键结论至少使用 5 个随机种子，报告均值±标准差或 95% 置信区间。

## 1. P0：数据协议和可训练基线

### 1.1 数据审计

**动作**：完成所有 `result_*.json` 生成；检查 JSON schema、单位、坐标系、节点映射、数组 shape/dtype/SHA-256、非有限值、温度步与静态位移索引是否一致。

**得到**：`data_dictionary.md`、样本统计表、质量审计报告、唯一 `data_version`。

**通过条件**：每个样本都能被 `StructuralDataset` 读取；静态为 `[n_temp, n_static_nodes, 3]`，动态为 `[n_time, n_dynamic_nodes, 3]`；标签长度一致；缺测有明确 mask。

### 1.2 严格切分和归一化

**动作**：固定 `seed=42` 作为示例，建立 `train/val/test` 的 state/group split；归一化统计量、阈值和校准参数只从训练/验证集计算。

**得到**：可保存的 split manifest 和 normalizer state；随机切分仅作为泄漏对照。

**通过条件**：三集合的状态/批次无交集；每个集合包含健康、材料、支座和复合状态，至少有足够正负样本。

### 1.3 最小可训练基线

**动作**：先分别训练 `static_only`、`dynamic_only` 和 `fusion`，保留当前 class-wise late fusion 作为基线；先关闭 warm start 和复杂增强，确认损失下降。

**得到**：每个模型的 checkpoint、训练曲线、验证/测试指标和运行配置。

**通过条件**：训练无 NaN；验证损失基本下降；输出包含材料、支座、区域和全桥任务；所有结果可追溯到数据版本、切分、种子和阈值。

## 2. P1：核心 AI 方法

### 2.1 静态和动态表示

**静态编码器**：温度对齐的位移序列 + 坐标 + 质量/mask，保留测点/区域 token；温度通过条件化 FiLM 或显式温度 token 注入。

**动态编码器**：加速度时程或时频 token + 坐标 + 采样率/激励/通道质量，保留测点/区域 token；当前 1D CNN 作为轻量起点，不同时堆叠 Transformer、GNN 和生成式补全。

**得到**：共享状态表示、模态私有表示和区域 token；同状态不同温度应更接近，不同状态应可分。

### 2.2 质量感知区域级融合

**动作**：在显式区域候选对应内加入轻量跨模态交互（static Query、dynamic Key/Value 或反向更新），门控输入包含两模态特征、质量向量、缺测 mask、区域位置和温度条件；保留单模态残差旁路。

**得到**：新模型与 late fusion 的统一 checkpoint 和 gate 统计。门控值只解释为信息注入比例，不直接解释为物理可靠性概率。

**通过条件**：在严格 state/group split、模态缺失和质量退化下，新模型相对 late fusion 的增益稳定。

### 2.3 层级多任务和一致性

**动作**：材料多标签、支座分类/回归、区域风险和全桥风险联合训练；加入全桥等级不低于区域最大等级的一致性损失。

**得到**：宏 F1、每类 F1、AUPRC、Exact Match、MAE/RMSE、加权 Kappa 的统一评估脚本。

## 3. P1 实验矩阵（AI 论文最低证据）

| 编号 | 实验 | 对照 | 主要问题 | 必须输出 |
|---|---|---|---|---|
| E1 | 单模态与融合 | Static-only、Vib-only、Concat、Late Fusion、新模型 | 融合是否真实增益 | 宏/每类 F1、AUPRC、Exact Match、MAE/RMSE |
| E2 | 模块消融 | 去位置、去温度、去质量门控、去交互、去层级损失、固定门控 | 每个模块是否必要 | 消融表、参数量、推理时间 |
| E3 | 防泄漏与泛化 | random split vs state/group split；未见状态组合/温度/激励 | 是否存在伪泛化 | 划分差异、5 seeds 均值±标准差 |
| E4 | 缺测和噪声鲁棒性 | 测点失活、模态缺失、噪声、InSAR 稀疏率梯度 | 质量退化下是否稳定 | 性能-扰动曲线、错误案例、校准变化 |

**判定规则**：若新模型只在随机切分提升、在严格切分下降，应报告为泄漏或过拟合，不得宣称泛化提升；若静态分支对构件损伤不敏感，应保留该负面结果并解释动态分支贡献。

## 4. P2：跨域学习和可信评估

### 4.1 FE 到缩尺实验

**动作**：按实验批次留出目标域，比较无适配、CORAL、MMD、少量目标域微调；对齐区域/状态特征而非原始信号。

**得到**：域间分类/分级指标、特征分布图、跨域错误类型和适配代价。

**通过条件**：目标域严格留出；适配后任务性能和校准至少有一项稳定改善，且没有以牺牲关键类别召回为代价。

### 4.2 概率校准和不确定性

**动作**：在验证集做温度缩放或逐类校准；比较校准前后 ECE、Brier、可靠性图、PR 曲线和不同阈值下误报/漏报；可选 MC Dropout 或深度集成。

**得到**：状态概率、异常区域、质量/缺测状态、不确定性和复核触发建议。

**边界**：高 F1 不能替代可信性；实桥没有损伤强标签时，只写异常辅助识别、状态一致性和复核触发。

## 5. P3：实桥案例和论文证据链

**动作**：接入 COSMO-InSAR、温度、运营振动和巡检/养护线索；分别输出 InSAR-only、Vib-only、Fusion，对异常区域和复核线索做一致性分析。

**得到**：典型相符/不相符案例、置信度、异常残差、复核建议和适用边界。

**不得声称**：没有强标签时，不输出精确损伤位置、损伤程度或多年退化趋势。

## 6. 每一步的结果台账

每张结果表和每幅图必须记录：`data_version`、模型/代码版本、数据域、split 策略、随机种子、超参数、阈值、校准方法、评估脚本和运行日期。建议统一保存到 `DL_model/logs/<model_type>/`，并用一个 JSON 汇总表作为论文结果唯一来源。

## 7. 当前代码使用和下一步

```powershell
# 先做 1 个 epoch 的链路检查
python DL_model/main.py --model static_only --epochs 1 --batch 2 --data-dir E:/working/DL_data/data_new

# 数据量足够后再跑完整基线
python DL_model/main.py --model all --data-dir E:/working/DL_data/data_new
```

当前目录只有 11 个样本，验证集可能没有正样本，因此只能用于代码烟雾测试。开始 E1-E4 前，应先补齐结构状态和各标签组合，确保每个严格留出集合都能计算宏 F1、AUPRC 和校准指标。
